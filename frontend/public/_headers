# Intentionally credential-free. Netlify Personal does not enforce the
# Basic-Auth custom response header, so access is enforced at runtime by
# netlify/edge-functions/basic-auth.js using the secret environment variable
# NETLIFY_BASIC_AUTH_CREDENTIALS. See SECURITY_AND_KEYS.md.

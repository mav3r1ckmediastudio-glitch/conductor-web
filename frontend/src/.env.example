# ── MapTiler ───────────────────────────────────────────────────────────────
# 1. Create a free account at https://www.maptiler.com
# 2. Copy your API key below
VITE_MAPTILER_KEY=your_maptiler_key_here

# Note: this app no longer uses Clerk for authentication (removed 1 Jul
# 2026). Access control is handled at the CDN level via Netlify Basic Auth
# — see frontend/public/_headers — not via any environment variable here.
